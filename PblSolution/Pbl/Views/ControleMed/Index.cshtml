@model IEnumerable<Pbl.Models.Med>
@{
    ViewBag.Title = "Index";
}

<h3>
    Controle Med
    <small> Pagina de controle Med</small>
</h3>


<div class="container-fluid">
    <!-- START DATATABLE 1 -->
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                </div>
                <div class="panel-body">
                    <div class="table-responsive">
                        <table id="datatable1" class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>MED</th>
                                    <th>Semestre</th>
                                    <th>Opções</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr class="gradeX">
                                        <td>MED @Html.DisplayFor(c => item.descMed)</td>
                                        <td>@Html.DisplayFor(c => item.Semestre.descSemestre)</td>
                                        <td>
                                            <i class="fa fa-cog "></i> @Html.ActionLink("Gerenciar", "GerenciarMed", new { id = item.idMed }, new { @class = "alter" })
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    @Styles.Render("~/bundles/datatablesCss")
}
@section Scripts {
    @Scripts.Render("~/bundles/datatables")
    @Scripts.Render("~/bundles/demoDatatable")
}



