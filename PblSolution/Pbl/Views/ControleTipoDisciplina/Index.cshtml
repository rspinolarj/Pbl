@model IEnumerable<Pbl.Models.TipoDisciplina>
@{
    ViewBag.Title = "Tipo disciplina";
}
<script type="text/javascript">
    $mensagem = "@ViewBag.Message";
    if ($mensagem) {
        alert($mensagem);
    }
</script>



<h3>
    Controle Tipos de Disciplinas
    <small> Pagina para Controle dos Tipos de Disciplinas</small>
</h3>


<div class="container-fluid">
    <!-- START DATATABLE 1 -->
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">

                    <a href="@Url.Action("Create", "ControleTipoDisciplina")">
                        <i class="fa fa-plus"></i>
                        Adicionar Tipo de Disciplina
                    </a>

                </div>
                <div class="panel-body">
                    <div class="table-responsive">
                        <table id="datatable1" class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Descrição</th>
                                    <th>Opções</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr class="gradeX">
                                        <td>@Html.DisplayFor(c => item.descTipoDisciplina)</td>
                                        <td>
                                            <button type="button" class="btn btn-sm btn-default" onclick="window.location = '@Url.Action("Update","ControleTipoDisciplina", new { id = item.idTipoDisciplina })'">
                                                <em class="fa fa-pencil"></em>
                                            </button>
                                            <button type="button" class="btn btn-sm btn-danger" onclick="window.location = '@Url.Action("Delete","ControleTipoDisciplina", new { id = item.idTipoDisciplina })'">
                                                <em class="fa fa-times"></em>
                                            </button>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Styles {
    @Styles.Render("~/bundles/datatablesCss")
}
@section Scripts {
    @Scripts.Render("~/bundles/datatables")
    @Scripts.Render("~/bundles/demoDatatable")
}





